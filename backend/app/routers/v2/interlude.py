"""DEPRECATED — do not use.

이 파일은 의도치 않게 v1 interlude 를 v2 로 중복 포팅하면서 만들어진 것으로,
사용자 피드백(중복 코드/프로세스 생성에 대한 반대)에 따라 폐기되었습니다.
v1 `backend/app/routers/interlude.py` 가 유일한 구현입니다.

파일 삭제 권한이 없어 내용만 비워 두었으며, `main.py` 에서도 import/include
가 모두 제거돼 있어 앱 동작에는 영향이 없습니다.
"""
"""이 파일은 더 이상 사용되지 않습니다. 내용을 비움."""
